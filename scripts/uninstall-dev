#!/usr/bin/env bash
# Remove the app and daemon, preserving loaded modules, RyzenAdj and settings.
set -euo pipefail
ROOT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
exec bash "$ROOT_DIR/scripts/uninstall" --app-only "$@"
